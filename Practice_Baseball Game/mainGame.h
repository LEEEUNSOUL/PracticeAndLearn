//mainGame이라는 클래스 생성됨 (헤더파일)

#pragma once //컴파일러 지시, 컴파일러에서는 '라이브러리' 역할을 함. 
#include <iostream>
#include <time.h>

using namespace std; 

class mainGame
{
public:
	mainGame(); //이 클래스가 호출되면 자동으로 실행되는 생성자 : 메모리 사용 선언 
	~mainGame(); // 클래스가 종료될 때 자동으로 실행하는 소멸자 : 메모리 해제 선언 



};

